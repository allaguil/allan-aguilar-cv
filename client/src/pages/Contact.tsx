import { FC } from 'react';
import { ContactFormGrid } from '../layouts/ContactFormGrid';

const Contact: FC = () => {
    return (
      <>
          <ContactFormGrid />
      </>
    )
  }

export default Contact;